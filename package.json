{
  "name": "vortex-website",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://www.vortex-tech.pt/",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && rimraf out && next build && next export && touch out/.nojekyll && touch out/CNAME && echo \"www.vortex-tech.pt\" >> out/CNAME && gh-pages -d out -t true"
  },
  "dependencies": {
    "@svgr/core": "^5.5.0",
    "@svgr/webpack": "^5.5.0",
    "bootstrap": "^4.6.0",
    "gh-pages": "^3.1.0",
    "next": "^10.0.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-icons": "^4.2.0",
    "reactstrap": "^8.9.0"
  },
  "devDependencies": {
    "babel-plugin-transform-define": "^2.0.0"
  }
}
